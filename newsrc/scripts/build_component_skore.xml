<?xml version="1.0" encoding="UTF-8"?>

<project default="build.all" basedir="./">

	<!--    ==================================================      -->
	<!--    initialize paths and properties                         -->
	<!--    ==================================================      -->

	<!-- initialization -->
	<target name="initialize">

		<!-- display ANT Environment -->
		<echo message="==> ANT Environment" />
		<echo message="ant.home         = ${ant.home}" />
		<echo message="ant.java.version = ${ant.java.version}" />
		<echo message="ant.version      = ${ant.version}" />

		<property name="project.name" value="Account Expiration Component" />
		<property name="deployment.dir" value="/home/skore/tmp/AEC_JoomlaANT/" />
		<property name="distribution.dir" value="/home/skore/tmp" />
		<property name="source.dir" value="./newsrc/code/" />

	</target>

	<!-- build all -->
	<target name="build.all" depends="initialize">
		<antcall target="deploy.dir" inheritall="true" />
		<antcall target="dist.generate" inheritall="true" />
	</target>

	<target name="deploy.dir">
		<copy todir="${deployment.dir}" overwrite="yes">
			<fileset dir="${source.dir}">
				<include name="*.*" />
			</fileset>
		</copy>
	</target>
	<target name="dist.generate">
		<zip destfile="${distribution.dir}/com_acctexp_0_12_6_2.zip" update="false">
			<fileset dir="${source.dir}" />
			<exclude name="build.xml" />
			<exclude name=".project" />
		</zip>
	</target>
</project>